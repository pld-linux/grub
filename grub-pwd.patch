Only in grub-0.91/stage2: Makefile.in.orig
Only in grub-0.91/stage2: Makefile.in.rej
diff -rU3 grub-0.91-vanilla/stage2/stage2.c grub-0.91/stage2/stage2.c
--- grub-0.91-vanilla/stage2/stage2.c	Tue Jan  8 03:56:45 2002
+++ grub-0.91/stage2/stage2.c	Wed Apr  3 08:13:28 2002
@@ -324,6 +324,7 @@
   char *cur_entry = 0;
   int disp_up = DISP_UP;
   int disp_down = DISP_DOWN;
+  int ask_pass = 0;
 
   /*
    *  Main loop for menu UI.
@@ -430,8 +431,13 @@
       grub_printf ("\n\
       Use the %c and %c keys to select which entry is highlighted.\n",
 		   disp_up, disp_down);
-      
-      if (! auth && password)
+
+      if (! auth && password && ask_pass) 
+	{
+	  ask_pass = 1;
+	  goto restart;
+        }  
+      else if (! auth && password)
 	{
 	  printf ("\
       Press enter to boot the selected OS or \'p\' to enter a\n\
